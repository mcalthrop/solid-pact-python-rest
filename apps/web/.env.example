# Optional. Overrides the API base URL for the generated client (see src/api/client.ts).
# If unset in dev, the client uses http://127.0.0.1:8000. If unset in production builds, requests are same-origin.
# VITE_API_BASE_URL=http://127.0.0.1:8000
